import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFFFA6002);
  static const background = Color(0xFFFBF6F0);

  // Attendance
  static const attendanceGreen = Color(0xFF22A06B);
  static const attendanceYellow = Color(0xFFD99A00);
  static const attendanceRed = Color(0xFFD64545);

  // Light mode
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightText = Color(0xFF1F1F1F);
  static const lightSecondaryText = Color(0xFF737373);
  static const lightBorder = Color(0xFFE8E1DA);

  // Dark mode
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1C1C1C);
  static const darkText = Color(0xFFF5F5F5);
  static const darkSecondaryText = Color(0xFFA6A6A6);
  static const darkBorder = Color(0xFF303030);
}